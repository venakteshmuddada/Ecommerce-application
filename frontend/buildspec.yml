version: 0.2

phases:
  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      - aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin <your_account_id>.dkr.ecr.us-east-1.amazonaws.com
  build:
    commands:
      - echo Building Docker image...
      - docker build -t ecommerce-frontend .
      - docker tag ecommerce-frontend:latest <your_account_id>.dkr.ecr.us-east-1.amazonaws.com/ecommerce-frontend-repo:latest
  post_build:
    commands:
      - echo Pushing image to ECR...
      - docker push <your_account_id>.dkr.ecr.us-east-1.amazonaws.com/ecommerce-frontend-repo:latest

artifacts:
  files: '**/*'
